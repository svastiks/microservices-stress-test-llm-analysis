FROM python:3.12-slim
WORKDIR /app
COPY mock_dep/requirements.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt
COPY mock_dep/ ./mock_dep/
ENV PYTHONUNBUFFERED=1
EXPOSE 8080
CMD ["python", "-m", "flask", "--app", "mock_dep", "run", "--host", "0.0.0.0", "--port", "8080"]
